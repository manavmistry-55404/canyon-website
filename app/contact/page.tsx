import { ContactContent } from "@/components/InnerPages";
import { SiteFrame } from "@/components/SiteFrame";

export default function ContactPage() {
  return (
    <SiteFrame>
      <ContactContent />
    </SiteFrame>
  );
}
